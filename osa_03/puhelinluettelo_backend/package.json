{
  "name": "puhelinluettelo_backend",
  "version": "0.0.2",
  "description": "3 moduulin tehtäviä",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "rm -rf build && cd ../puhelinluettelo_for_3 && npm build && cp -r build ../puhelinluettelo_backend",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx nodemon app.js"
  },
  "author": "Petri Mattila",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
